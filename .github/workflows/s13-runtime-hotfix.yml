name: S13 M2 Runtime Hotfix

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'S13 runtime hotfix already deployed and verified; automatic PR redeploy retired.'
