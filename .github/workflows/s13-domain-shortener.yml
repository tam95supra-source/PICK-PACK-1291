name: S13 Workers Dev Hostname Shortener

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Initial hostname workflow retired after safe pre-change stop; use S13 Workers Dev Hostname Finalize.'
