name: Service M2 Beta20 Run Probe

on:
  pull_request:
    branches: [ main ]
    paths:
      - '.github/workflows/service-m2-beta20-run-probe.yml'
  workflow_dispatch:

permissions:
  contents: read
  actions: read

jobs:
  probe:
    if: github.event_name != 'pull_request' || github.head_ref == 'agent/service-migration-m2'
    runs-on: ubuntu-latest
    env:
      GH_TOKEN: ${{ github.token }}
      TARGET_HEAD_SHA: 3c94e41820a49dde19619ced4d473ca83f88e7fa
    steps:
      - name: Print exact Beta20 direct Drive publisher run
        shell: bash
        run: |
          set -euo pipefail
          DATA=$(gh api "/repos/${GITHUB_REPOSITORY}/actions/workflows/service-m2-beta20-drive-direct-publish.yml/runs?branch=agent%2Fservice-migration-m2&per_page=20")
          node - <<'NODE' "$DATA"
          const j=JSON.parse(process.argv[2]);
          const x=(j.workflow_runs||[]).find(r=>r.head_sha===process.env.TARGET_HEAD_SHA&&r.event==='push');
          if(!x){console.error('BETA20_DIRECT_RUN_NOT_FOUND');process.exit(1)}
          console.log(`BETA20_DIRECT_RUN_ID=${x.id}`);
          console.log(`BETA20_DIRECT_STATUS=${x.status}`);
          console.log(`BETA20_DIRECT_CONCLUSION=${x.conclusion||''}`);
          NODE
