# HANDOVER S09 — Pick Pack 1291

Status: **IMMUTABLE SESSION SNAPSHOT**  
Closed: **2026-08-18 12:42 +07:00 (Asia/Bangkok)**  
Next session: **S10**

> **S10 STOP RULE:** Read this snapshot together with `docs/HANDOVER_CURRENT.md`, `AGENTS.md`, `ARCHITECTURE_GUARDRAILS.md`, `docs/UI_UX_SYSTEM.md`, `docs/ADMIN_ACCOUNT_RULES.md`, `docs/BUILD_RELEASE_PLAYBOOK.md` and `docs/HANDOVER_POLICY.md`. Report that the state is understood, then **WAIT FOR A NEW OWNER COMMAND**. Do not mutate source, Sheet, GAS, Drive, OTA or Stable merely because the session opened.

## 1. Architecture remains owner-locked

`Android App ↔ Google Apps Script Web App ↔ Google Sheets`

- Sheets = operational source of truth.
- GAS = transaction/API bridge.
- GitHub = source/CI/release infrastructure only.
- No Supabase/Firebase/Neon/Cloudflare/other DB/backend/auth/sync authority without explicit owner command.
- Beta is full-function and uses real business rules/data.
- Drive mutations stay inside official `PICK PACK 1291 - CHÍNH THỨC` tree.

Owner workstation cannot run local CLI. Never tell owner to run git/gh/clasp/adb/Gradle/npm/Java/OpenSSL/Terminal commands; move CLI work to CI/assistant tooling.

## 2. Current authoritative data rules

Workbook: `DỮ LIỆU THEO NGÀY`.

Active tabs include `Danh mục`, personnel/PDA/PICK/PACK masters, `RA - VÀO TRONG CA`, `CÔNG NHẬT`, and `Danh sách Admin`.

### Catalog rule locked in S09

`Danh mục` headers use `SHEET_FIELD` semantics.

- A catalog belongs only to the matching sheet + field.
- Never borrow values from another sheet because field names look similar.
- System-owned statuses are hidden from contexts where the user is not allowed to edit them.
- `DANH SÁCH PDA_Tình trạng` is not selectable while assigning PDA to PICK.
- No implicit fallback when a catalog is absent unless owner defines it.

### Admin namespace owner lock

`Danh sách Admin_Vị trí` is **not catalog-driven**. The only valid values are:

- `superadmin`
- `admin`
- `user`

No cross-sheet fallback is permitted. Backend derives/validates position from role. Normal app account creation cannot create another SUPERADMIN unless owner explicitly changes this rule.

## 3. Business/auth invariants still active

- MNV is business key.
- Session: `MNV + business_date`, `NOT_ENTERED -> ACTIVE -> ENDED`.
- ENDED cannot normally re-enter same business day.
- Mutations use immutable/idempotent event IDs.
- Exclusive resources are race-safe and resource change is atomic.
- PICK requires PDA; User Pick is optional.
- PDA flow uses last 5 serial digits + validated suggestions.
- PACK keeps shift mapping/exclusivity.

Roles: SUPERADMIN / ADMIN / USER; backend enforces permissions.

Auth/session:

- PBKDF2-HMAC-SHA256 verifier
- challenge/HMAC login proof
- `SINGLE_ACTIVE_DEVICE_V1`
- session survives app/process closure on same installation
- second-device login replaces previous active server session
- forgot-password reset mail uses account `Mail` field

## 4. UI state after real-device review

Approved design family remains modern enterprise blue/indigo/violet with centralized 7-color theming. Do not redesign away from it without owner instruction.

S09 real-device corrections:

- Remove duplicate root-tab titles from gradient header.
- Header: no avatar placeholder; exactly three left-aligned lines — display name, position, login ID.
- No `Tài khoản:` prefix.
- Compact right side is Mạng / Đồng bộ / Service only.
- Tab switch must not reset network state to `Đang kết nối`.
- Five root tabs stay in one persistent Activity shell; no artificial transition delay.
- Staff list renders incrementally/lazily; search still covers complete local master cache.
- Semantic Android vector icons required.
- Work cards stay equal.
- Settings has no duplicate sync-data section.
- 7 color swatches stay one row.
- User-facing copy contains only information needed by ordinary users; no AI/developer/protocol commentary.

## 5. Notifications/logging/reports

Notifications:

- routine top notices ~3 seconds
- maximum 3 queued/visible
- routine statuses do not require OK dialogs
- manual log/destructive actions may require confirmation
- scanner Enter/OK triggers action without redundant `Kiểm tra` button

Logs:

- MANUAL / CRASH / DAILY categories remain
- local log deleted only after successful server acknowledgement for that upload/event
- pending crash/daily logs are retried after restored session

Reports retain current compact/coherent layout rules, support block hiding at zero, Phúc Long before Kéo hàng and zero-value optimizations.

## 6. Current live release

Published Beta: **`0.4.2-beta.9` / versionCode 15**.

- Package: `vn.pickpack1291.app.beta.publicbeta`
- APK name: `pick-pack-1291-public-beta-v0.4.2-beta.9.apk`
- SHA-256: `6c96a9415299bd11f73ed21e314fb354c530c093f30ae1e23bfa7332d0ff3b6b`
- Fixed signer SHA-256: `d180450ae47ac6e8daf26840308e62bd602d5f8d6ac12ee0da58e5eb1a44731e`
- OTA discovery: GAS `update_check` -> official Beta Drive folder.

Beta9 release gates passed: Debug builds, explicit GAS deploy/live validation, Release Preflight, fixed signer check, Beta8->Beta9 discovery, live download SHA match, self-update false, Stable isolation.

Stable is **not promoted** and requires explicit owner approval after Beta soak/business acceptance.

## 7. Build/OTA optimization completed at S09 close

Owner reported that prior “optimization” still felt slower because temporary workflows/status probes/docs finalizers were on the elapsed path. This was corrected.

### Permanent workflow set is now five

1. `App Fast Check`
2. `Release Preflight - Beta and Stable`
3. `Deploy Current GAS`
4. `Verify Google Apps Script Credentials`
5. `Verify Beta OTA`

No per-release observer/status/finalizer/OTA workflow should be created.

### Fast Check now scopes work

- App/Gradle change -> static + Beta Debug + Stable Debug.
- GAS-only change -> static/GAS syntax; Android build skipped.
- Docs/handover-only change -> does not trigger Android Fast Check.
- Old Fast Check is cancelled when a newer source commit arrives.

### Release Preflight now parallelizes

- live GAS/Drive checks run in parallel with Android Release build
- Beta Release + Stable Release assembled once
- authoritative unsigned Beta artifact + release metadata uploaded
- sign/publish must reuse that artifact; no rebuild before OTA

### Permanent Beta OTA verification

`ops/beta-ota-verify-trigger.txt` drives the permanent `Verify Beta OTA` workflow. It validates:

- previous Beta -> target discovery
- Drive/Beta source/channel
- actual download SHA
- package/version
- fixed signer
- self-update false
- Stable isolation

No generated status JSON is required.

### Critical path now locked

Android-only:

`source -> Fast Check -> Release Preflight -> reuse artifact -> sign -> Drive upload -> Verify Beta OTA -> RELEASED`

Android + GAS:

`source -> Fast Check -> Deploy Current GAS -> Release Preflight -> reuse artifact -> sign -> Drive upload -> Verify Beta OTA -> RELEASED`

Handover/docs/history cleanup happen after OTA PASS, not before release completion.

## 8. Remaining release-speed bottleneck

The four normal Android signing secrets are still not confirmed complete:

- ANDROID_SIGNING_KEY_B64
- ANDROID_SIGNING_STORE_PASSWORD
- ANDROID_SIGNING_KEY_PASSWORD
- ANDROID_SIGNING_ALIAS

Until owner later configures them browser-only, the approved fallback is:

- reuse Preflight artifact
- use official encrypted signing recovery material in temporary assistant runtime
- verify fixed signer
- delete decrypted material immediately
- upload through approved Drive connector
- run permanent OTA verifier

Do not create a new signing identity, temporary signing bridge, temporary signing workflow or unrelated external signing service.

One-time configuration of the four GitHub signing secrets is the remaining major step toward true one-click signing/publish preparation.

## 9. Recurring failures future sessions must not repeat

- Do not assume `./gradlew`; use `gradle/actions/setup-gradle` + `gradle`.
- Do not embed giant source patches inside workflow YAML.
- Do not dispatch a workflow in the same commit that first creates it.
- Do not create observer/status workflows writing to `main`.
- Do not let CI self-edit `.github/workflows`.
- Do not hardcode old Beta versions.
- Do not run full release/live gates for every small edit.
- Do not bootstrap Android SDK if exact hosted tools are already present.
- Do not assume GAS OAuth has Drive-upload scope.
- Do not explore historical/unapproved external backends.
- Do not put docs/handover finalization on OTA critical path.

## 10. Next session

S10 should first read authoritative files and wait for owner command.

Likely testing areas if owner requests them:

- real-device Beta9 visual fit
- network status persistence across tabs
- Staff large-list responsiveness
- strict catalog selections in forms
- Admin role/position lock
- QR/Công nhật/PICK/PACK/report business acceptance

Do not promote Stable without explicit owner approval.
