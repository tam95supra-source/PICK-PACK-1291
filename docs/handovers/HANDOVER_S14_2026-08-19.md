# HANDOVER S14 — BETA22 CORRECTED BASELINE — PICK PACK 1291

Date: **2026-08-19**  
Purpose: immutable supersession snapshot correcting stale Beta19-as-current wording without rewriting S12/S13 history.

## 1. Supersession

S12/S13 statements that Beta19 was the latest release remain historical facts about those session closures, but are **SUPERSEDED as current release instructions**.

Current verified Android Beta baseline:

- versionName `0.4.2-beta.22`
- versionCode `28`
- package `vn.pickpack1291.app.beta.publicbeta`
- verified OTA SHA-256 `da2c1c837102e9e557013971ece9dac961caa6312b579db9c63184acc8daed3b`
- verified OTA size `9790372` bytes

Stable remains **UNPUBLISHED / UNTOUCHED**. Android signing identity remains OWNER-locked.

## 2. Authoritative architecture

OWNER-approved production architecture:

`Android / Web-PWA ↔ Cloudflare Worker Service ↔ D1`

with Durable Objects/WebSocket realtime, Google Sheets operational replica/compatibility/fallback/DR, GAS discovery/compatibility/fallback/OTA bridge, Android SQLite/cache local projection, and GitHub source/CI/release.

`ARCHITECTURE_GUARDRAILS.md` explicitly supersedes the older GAS-only architecture wording. During S14 correction, `AGENTS.md` was reconciled to the Service-first architecture while preserving non-conflicting business/auth/UI/release/signing constraints.

## 3. Last verified pre-failback production snapshot — revalidate before action

Last successful Session1 live postcheck recorded:

### Service

- environment `production`
- generation `m2-prod-20260819-001`
- mode `SERVICE_PRIMARY`
- epoch `6`
- seq `3`
- scope `PRODUCTION`
- replication `HEALTHY`, pending `0`
- reconciling `0`
- epoch-7 fallback inbox `0`

### GAS / Google fallback

- mode `GOOGLE_FALLBACK`
- epoch `7`
- seq `3`
- matching generation
- exactly 3 epoch-7 PENDING fallback events, seq 1–3
- all three current pending events originated from Beta22

No 7→8 failback had started at the correction checkpoint. Target epoch 8 is a controlled plan only, not permission to execute.

## 4. S14 corrective changes completed in repository

- `AGENTS.md` architecture corrected to Service-first.
- `docs/HANDOVER_CURRENT.md` corrected to Beta22 current baseline.
- Legacy Beta19/Beta20 OTA publisher workflows are being retired as historical no-op workflows; historical evidence remains in git history.
- Both M2 Chaos workflows now apply an exact-marker current-contract patch before local isolated execution.
- Chaos correction preserves runtime rules instead of weakening them:
  - ADMIN/USER N/N-1 write window;
  - SUPERADMIN current 7-business-day service/PDA window;
  - `M1_SHADOW_PROBE` SUPERADMIN-only;
  - realtime `INVALIDATION_V1` / `DAY_CHANGED` rather than legacy `DELTA`.

Fresh CI evidence is still required before calling the corrected Chaos suites PASS.

## 5. Historical work preserved

Do not regress or remove valid version-neutral implementation:

- Worker/D1 canonical Service-first architecture and migrations;
- PWA `INVALIDATION_V1` + `DAY_CHANGED` realtime contract;
- recovery/failback route and fencing;
- fallback payload sanitization + checksum compatibility;
- constant-time bridge-secret helper;
- CI storm cleanup;
- Worker deploy/source-gate repairs;
- import backend engine;
- Stable/signer/data safety locks.

Beta19/Beta20 OTA workflows/events/evidence remain historical only and must not be reused as the current publisher path.

## 6. Current test/CI interpretation

Historical successful Session1 evidence remains evidence for those runs. The later Chaos Matrix and Chaos V2 failures were stale-contract failures, not proof production Service was down.

Known old failure point: case 17 sent an ADMIN `M1_SHADOW_PROBE`, so current runtime correctly returned `SHADOW_PROBE_SUPERADMIN_REQUIRED` before the old test could reach the N/N-1 window assertion.

The corrected test path uses legitimate business mutations to test the date window and uses `DAY_CHANGED` realtime invalidations. Do not revert production behavior to satisfy obsolete assertions.

## 7. Release Preflight debt

Release Preflight already reads Android version metadata from source and preserves fixed signer validation, but its GAS live gate still includes historical compatibility labels such as `APP_GSHEET`, S12 report engine, and S13 history engine.

Those checks must either be clearly treated as compatibility-only or updated so preflight explicitly proves Service-first production plus GAS OTA/compatibility health. A PASS must not imply GAS/Sheets are current operational authority.

## 8. Beta22 acceptance

Real Beta22 device operations are evidenced by the production fallback ledger, but that does not prove every formal acceptance row.

Do not automatically mark real-PDA or Web+Beta22 concurrent acceptance PASS. Reconstruct existing evidence first and only execute genuinely missing tests.

## 9. Failback gate — no execution from this snapshot

Before failback, freshly verify at one checkpoint:

- branch head / PR #38 state;
- Service authority/generation/replication;
- GAS fallback authority;
- exact pending fallback count/sequence/generation;
- D1 fallback inbox/recovery lock;
- no concurrent mutating deploy/recovery workflow;
- Stable remains forbidden;
- PR remains draft/unmerged.

Mismatch means abort the risky transition and report; never force live state to the handover.

## 10. Safety locks

- Do not publish Stable without explicit OWNER command.
- Do not merge PR #38 until full DoD and explicit OWNER approval.
- Do not replace Android signer.
- Do not overwrite/delete production Google Sheet data.
- Do not expose secrets or raw legacy sensitive fallback fields.
- Do not ask OWNER to use local CLI.
- Do not invent a new backend/datastore/queue/auth/sync architecture.

## 11. Continuation order

1. Complete publisher/preflight/doc/PR reconciliation.
2. Obtain fresh CI evidence for corrected Chaos tests.
3. Reconstruct Beta22 formal PDA/Web acceptance evidence.
4. Revalidate live Service/GAS/D1/Google fallback state.
5. Only then consider controlled failback to the planned next epoch.
6. Complete DR both directions, final integrity verification, and production-complete handover.
